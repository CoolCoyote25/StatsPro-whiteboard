import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.basketball.whiteboard',
  appName: 'StatsPro Whiteboard',
  webDir: 'dist'
};

export default config;
